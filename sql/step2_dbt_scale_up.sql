-- =============================================================================
-- dbt 実行前: dbt を速く動かすため、計算する力を大きくする 🚀
-- =============================================================================
-- この SQL は dbt を実行する直前に、ファイル全体を実行します。
--
-- ウェアハウスは「計算を担当する機械」です。
-- XSMALL から LARGE に変えても、テーブルやデータの内容は変わりません。
-- dbt が使う BCAST_HANDSON_WH を一時的に大きくして、待ち時間を短くします。
-- =============================================================================

USE ROLE ACCOUNTADMIN;

ALTER WAREHOUSE BCAST_HANDSON_WH
  SET WAREHOUSE_SIZE = 'LARGE';

-- size が LARGE と表示されたら準備完了です。
SHOW WAREHOUSES LIKE 'BCAST_HANDSON_WH';

-- 次は dbt_project.yml を開き、dbt の「実行」を押してください。
